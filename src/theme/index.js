import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  primary: "#ee4d2d",
  // secondary: ,

  text: {
    primary: "#333333",
    secondary: "#636363",
  },
});

export default theme;
